server:
  port: 8080


limit:
  minMoney: 0.01
  maxMoney: 9999
  description: 最少要发 ${limit.minMoney}元,最多${limit.maxMoney}元。hh
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/luckymoney?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false